<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="SysUserMapper">
	
	<resultMap id="Result" type="">
		<result property="userId" column="USER_ID" jdbcType="DECIMAL"/>
		<result property="userName" column="USER_NAME" jdbcType="VARCHAR"/>
		<result property="userPassword" column="USER_PASSWORD" jdbcType="VARCHAR"/>
		<result property="userLock" column="USER_LOCK" jdbcType="CHAR"/>
		
	</resultMap>
	 

	<select id="queryAllInfo" resultMap="Result" parameterType="java.util.HashMap" >
				 select 
					base.USER_ID,
					base.USER_NAME,
					base.USER_PASSWORD,
					base.USER_LOCK
				 from SYS_USER base 
		 <trim prefix="where" prefixOverrides="and |or"> 
			<if test="userId != null and userId !='' ">  and USER_ID = #{userId}#</if>
			<if test="userName != null and userName !='' ">  and USER_NAME = #{userName}#</if>
			<if test="userPassword != null and userPassword !='' ">  and USER_PASSWORD = #{userName}#</if>
			<if test="userLock != null and userLock !='' ">  and USER_LOCK = #{userLock}#</if>
		</trim>
		ORDER BY USER_ID  DESC 
	</select>

	<select id="queryInfo" resultMap="Result" parameterType="java.util.HashMap" >
		<include refid="pagination.paginationStart"></include>
				 select 
					base.USER_ID,
					base.USER_NAME,
					base.USER_PASSWORD,
					base.USER_LOCK
				 from SYS_USER base 
		 <trim prefix="where" prefixOverrides="and |or"> 
			<if test="userId != null and userId !='' ">  and 	USER_ID = #{userId}#</if>
			<if test="userName != null and userName !='' ">  and 	USER_NAME = #{userName}#</if>
			<if test="userPassword != null and userPassword !='' ">  and 	USER_PASSWORD = #{userName}#</if>
			<if test="userLock != null and userLock !='' ">  and 	USER_LOCK = #{userLock}#</if>
		</trim>
		ORDER BY USER_ID  DESC 
		<include refid="pagination.paginationLast"></include>
	</select>

	<select id="queryCountInfo" resultType="integer" parameterType="java.util.HashMap" >
				<!--vw_t_gczlgl_info 前台查询视图  ; vwm_t_gczlgl_info = 后台管理查询视图-->
				<!--注意： where 条件后字段前必须有空格-->
				<!--select count(N_XH) from vw_t_gczlgl_info-->
				<!--select count(N_XH) from vwm_t_gczlgl_info-->
				select count(base.USER_ID) from SYS_USER
 		<trim prefix="where" prefixOverrides="and |or"> 
			<if test="userId != null and userId !='' ">  and 	USER_ID = #{userId}#</if>
			<if test="userName != null and userName !='' ">  and 	USER_NAME = #{userName}#</if>
			<if test="userName != null and userName !='' ">  and 	USER_PASSWORD = #{userName}#</if>
			<if test="userLock != null and userLock !='' ">  and 	USER_LOCK = #{userLock}#</if>
		</trim>
	</select>

	<insert id="saveInfo" parameterType="HashMap" >
			INSERT INTO SYS_USER(
				 USER_ID
		    <trim  prefixOverrides=","> 
			<if test="userName != null and userName !='' ">  and USER_NAME</if>
			<if test="userPassword != null and userPassword !='' ">  and USER_PASSWORD</if>
			<if test="userLock != null and userLock !='' ">  and  USER_LOCK</if>
			</trim>
			)VALUES(
				SEQ_SYS_USER_ID.NEXTVAL 
				<!-- #{n_xh#-->
			<trim  prefixOverrides=","> 
			<if test="userName != null and userName !='' ">  and  #{userName}#</if>
			<if test="userPassword != null and userPassword !='' ">  and  #{userPassword}#</if>
			<if test="userLock != null and userLock !='' ">  and  #{userLock}#</if>
			</trim>
			)
		<selectKey resultType="string" keyProperty="id" >
			 <!--SELECT #{nXh# AS ID FROM dual  -->
			 SELECT  SEQ_SYS_USER_ID.CURRVAL  AS ID FROM dual 
		</selectKey>
	</insert>

	<insert id="saveAllInfo" parameterType="java.util.HashMap" >
			INSERT INTO SYS_USER(
				USER_ID,
				USER_NAME,
				USER_PASSWORD,
				USER_LOCK
			)VALUES(
				 SEQ_SYS_USER_ID.NEXTVAL,  
				<!--#nXh#,-->
				#{userName}#,
				#{userPassword}#,
				#{userLock}#
			)
		<selectKey resultType="string" keyProperty="id" >
			 <!--SELECT #{nXh# AS ID FROM dual --> 
			 SELECT  SEQ_SYS_USER_ID.CURRVAL  AS ID FROM dual 
		</selectKey>
	</insert>

	<update id="updateInfo" parameterType="java.util.HashMap"> 
			update SYS_USER 
			<trim prefix="set" prefixOverrides=","> 
				<if test="userName != null and userName !='' "> , USER_NAME = #{userName}#</if>
				<if test="userPassword != null and userPassword !='' "> , USER_PASSWORD = #{userPassword}#</if>
				<if test="userLock != null and userLock !='' "> , USER_LOCK = #{userLock}#</if>
			</trim>
			<trim prefix="where" prefixOverrides="and |or">  
				<if test="userId != null and userId !='' ">  and USER_ID = #{userId}#</if>
				<if test="userIds != null and userIds !='' ">  and  USER_ID in (${userIds}$)</if>
			</trim>
	</update> 

	<update id="updateAllInfo" parameterType="java.util.HashMap"> 
			update SYS_USER set 
				USER_ID = #{userId}#,
				USER_NAME = #{userName}#,
				USER_PASSWORD = #{userPassword#},
				USER_LOCK = #{userLock}#
			WHERE 
				USER_ID = #{userId)#
	</update> 

	<delete id="removeInfo" parameterType="java.util.Map"> 
			 DELETE FROM SYS_USER
			 WHERE
			<!--N_XH = #{nXh#--> 
			<![CDATA[ USER_ID IN (${userIds}$) ]]>
	</delete> 
</mapper>